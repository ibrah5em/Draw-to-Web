% SOURCES: docs/0.2.0v/plan.md (§10 Tasks, §17 Milestones, §9 Library-to-Feature Map);
%          CHANGELOG.md; docs/1.0.0v/supervisor-report.md (§1)
% GUIDANCE: The plan you FOLLOWED. A milestone table + a short narrative is ideal.

\chapter{Project Implementation Plan}
\label{ch:plan}

\section{Milestone Breakdown}
% DRAFT (review): compact version of plan.md §17.
The work was divided into five gated milestones plus the v1.0.0 demo cut
(Table~\ref{tab:milestones}). Each milestone was ``done'' only when its gating
tasks were complete and all quality gates were green.

\begin{table}[H]
  \centering
  \caption{Milestone plan and definition of done.}
  \label{tab:milestones}
  \begin{tabularx}{\linewidth}{@{}l l X@{}}
    \toprule
    \textbf{Milestone} & \textbf{Theme} & \textbf{Definition of done (summary)} \\
    \midrule
    M1 & Foundation            & Types + store + recursive canvas + undo/redo + persistence + basic generator + export shell. \\
    M2 & Tokens + Themes       & Token registry + theme toggle + auto-layout + \texttt{:root} block + \texttt{var()} refs. \\
    M3 & Composition + Resp.   & 6+ presets + hover/focus states + breakpoint switcher + multi-select + image upload. \\
    M4 & Runtime + Hardening   & Full runtime snippets + SEO + validation console + \texttt{axe-core} gate + templates. \\
    M5 & Polish + Demo         & Polish + performance budgets + release artifact + rehearsed demo. \\
    v1.0.0 & Demo cut          & draw-to-create + match-layout + headless MCP server; released with installers. \\
    \bottomrule
  \end{tabularx}
\end{table}

% TODO: if your programme requires a Gantt chart, add one here (e.g. package
% `pgfgantt`). The task IDs and dependencies live in plan.md §10.

\section{Task Allocation}
% SOURCES: docs/0.2.0v/plan.md (§10); CLAUDE.md (Module Ownership)
% DRAFT (review):
Tasks were prefixed by owner and area (for example \texttt{I-GEN-*} for the
generator, \texttt{I-RUN-*} for runtime snippets), each with a priority and
explicit \texttt{depends:}/\texttt{blocks:} links so the critical path was
visible. The Document Model landed first because it blocked every other area.
% TODO: reproduce a representative slice of the task list if required.

\section{Deviations from Plan}
% SOURCES: DECISIONS.md (D-01..D-04)
% DRAFT (review): honest account of what changed, backed by the decision log.
Two planned items were consciously cut or deferred, both recorded in the project
decision log: code signing was cancelled permanently (no certificates,
\textbf{D-01}); and the v0.3.0/v1.0.0 releases shipped before the manual demo
rehearsal and render-layer performance sign-off (\textbf{D-02}, \textbf{D-04}).
The three Tier-2 authoring surfaces were brought forward into v1.0.0
(\textbf{D-03}). % TODO: reflect on why, in your own words.
