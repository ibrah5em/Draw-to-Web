Start the development environment:

1. Run `npm run dev` to launch Electron in dev mode (hot reload for main + renderer).
2. Confirm the app window opens.
3. Report Electron, Node, and Vite versions.
4. Tail the dev server output for the first 5 seconds and report any warnings.
